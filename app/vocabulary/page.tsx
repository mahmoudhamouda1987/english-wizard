"use client";
import { useState } from "react";
const words=["appointment","routine","introduce","country","family","station","schedule","conversation"];
export default function VocabularyPage(){const [saved,setSaved]=useState<string[]>([]);async function mark(word:string){const id=localStorage.getItem("english-wizard-learner-id");if(!id)return;await fetch("/api/practice/submit",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({learnerId:id,skill:"vocabulary",objectiveId:"a1-self-introduction-speaking",correct:true,evidenceId:`vocab-${word}`})});setSaved([...saved,word])}return <main style={{maxWidth:900,margin:"0 auto",padding:48}}><p className="eyebrow">Vocabulary builder</p><h1>Useful words for everyday English</h1><p className="subtle">Mark words you can actively use; the action is recorded as learner evidence.</p><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(180px,1fr))",gap:12,marginTop:24}}>{words.map(w=><button key={w} onClick={()=>mark(w)} style={{padding:18,borderRadius:14,border:"1px solid #dfe3ec",background:saved.includes(w)?"#eef9f2":"white",textAlign:"left"}}><strong>{w}</strong><br/><small>{saved.includes(w)?"Recorded ✓":"I know this word"}</small></button>)}</div></main>}
