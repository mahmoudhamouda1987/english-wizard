import { MVP_LESSONS } from "./curriculum";

/**
 * Production curriculum export. MVP_LESSONS is retained as a backward-compatible
 * name for existing imports, but it now contains the complete Pre-A1→C2 lesson set.
 */
export const ALL_LESSONS = [...MVP_LESSONS];
